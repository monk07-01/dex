syntax = "proto3";
package dex.coinfactory;

import "amino/amino.proto";
import "gogoproto/gogo.proto";

option go_package = "dex/x/coinfactory/types";

// Params defines the parameters for the module.
message Params {
  option (amino.name) = "dex/x/coinfactory/Params";
  option (gogoproto.equal) = true;

  
}